WITH RECURSIVE H AS (
    SELECT 0 AS TIME
    UNION ALL
    SELECT H.TIME + 1 FROM H WHERE TIME < 23
)

SELECT H.TIME, COUNT(HOUR(OUTS.DATETIME))
FROM H
LEFT JOIN ANIMAL_OUTS OUTS
ON H.TIME = HOUR(OUTS.DATETIME)
GROUP BY H.TIME;
